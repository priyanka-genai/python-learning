git checkout main
git pull

# To switch back to your new branch

git checkout -b feature-patterns
git add .
git commit -m "Add pattern programs"
git push -u origin feature-patterns


git switch -c Python